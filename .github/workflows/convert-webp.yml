name: Convert Images to WebP

on:
  push:
    paths:
      - 'assets/images/uploads/**'

jobs:
  convert:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Install WebP tools
        run: sudo apt-get install -y webp

      - name: Convert new images to WebP
        run: |
          find assets/images/uploads -type f \( -iname "*.jpg" -o -iname "*.jpeg" -o -iname "*.png" \) | while read img; do
            webp_file="${img%.*}.webp"
            if [ ! -f "$webp_file" ]; then
              cwebp -q 80 "$img" -o "$webp_file"
              echo "Converted: $img → $webp_file"
            fi
          done || true

      - name: Remove original images
        run: |
          find assets/images/uploads -type f \( -iname "*.jpg" -o -iname "*.jpeg" -o -iname "*.png" \) | while read img; do
            webp_file="${img%.*}.webp"
            if [ -f "$webp_file" ]; then
              rm "$img"
              echo "Removed: $img"
            fi
          done || true

      - name: Commit WebP images
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A assets/images/uploads/
          git diff --staged --quiet || git commit -m "🖼️ Convert images to WebP"
          git push
